[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "nba-lab"
version = "0.1.0"
description = "Point-in-time NBA simulation and counterfactual analysis lab"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.34,<1", "pydantic>=2.10,<3", "numpy>=2,<3"]

[project.optional-dependencies]
dev = ["pytest>=8,<10", "httpx>=0.28,<1"]
data = ["nba_api>=1.10,<2"]
impact = ["pbpstats>=1.3.11,<2"]

[project.scripts]
nba-lab = "nba_lab.cli:main"
nba-lab-sync = "nba_lab.cli:sync"
nba-lab-sync-awards = "nba_lab.cli:sync_awards"
nba-lab-sync-impact = "nba_lab.cli:sync_impact"
nba-lab-sync-replay = "nba_lab.cli:sync_replay"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.setuptools.package-data]
nba_lab = ["static/*"]
