name: iteration-check

on:
  push:
    branches: [codex/iteration-2]

concurrency:
  group: iteration-check-2
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install
        id: install
        continue-on-error: true
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'
      - name: JavaScript syntax
        id: js
        continue-on-error: true
        run: node --check src/nba_lab/static/app.js
      - name: Tests
        id: tests
        continue-on-error: true
        run: pytest -q
      - name: Report
        run: |
          echo "install_outcome=${{ steps.install.outcome }}"
          echo "js_outcome=${{ steps.js.outcome }}"
          echo "tests_outcome=${{ steps.tests.outcome }}"
